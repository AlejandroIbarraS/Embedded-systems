It's a server for a Raspberry Pi 3

This server uses a Raspberry alongside a camera, when a client makes a request, the server take a picture, then using parallel and concurrent programming process the image using the Sobel filter, then it send the image to the cliente.
 